"""
URL routes for the `tools` app.

Will host the intelligence tool invocation endpoints (lead score, outreach
brief, etc.) — added from Phase 5 onward.
"""

from django.urls import path

app_name = 'tools'

urlpatterns: list[path] = []
