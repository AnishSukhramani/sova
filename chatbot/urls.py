"""
URL routes for the `chatbot` app.

Will host the LangGraph-backed conversation endpoints — added in Phase 10.
Scaffolded now so the URL namespace exists from day one.
"""

from django.urls import path

app_name = 'chatbot'

urlpatterns: list[path] = []
